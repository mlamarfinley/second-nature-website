node_modules/
dist/
.DS_Store
.env
.env.local

# Internal planning docs must never land in the published folder again.
# docs/ is what GitHub Pages serves — it is a build artifact, not a workspace.
docs/superpowers/
notes/
